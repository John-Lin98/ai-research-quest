[English](PRIVACY.md) | **简体中文**

# 隐私说明

AI Research Quest 是一个静态、客户端运行的 Demo。

## 数据流

- 应用没有账号系统、分析、遥测或应用服务器。
- 浏览器加载静态 HTML、CSS、JavaScript 和公开 Demo 数据后，应用代码不会把用户输入发送到网络。
- 当前状态和考试自由文本只保存在页面内存中，不写入 `localStorage`、`sessionStorage` 或 IndexedDB。
- 刷新或关闭页面会清除内存会话。
- 只有用户主动点击导出按钮后才会下载文件。浏览器在本地生成 `Blob` 并保存到用户设备，应用不会上传该文件。

静态托管服务商在提供页面资源时仍可能按其政策接收普通请求元数据。AI Research Quest 不会在这些请求中加入用户输入、遥测标识或分析事件。

## 安全输入

只使用公开、模拟、改编或已经脱敏的内容。不要输入：

- 邮箱、姓名或其他个人标识；
- 密码、API Key、访问令牌或凭据；
- 私有绝对路径、服务器信息或内部 URL；
- 私有代码、未公开结果、数据集或模型 checkpoint。

导出检查会拒绝常见的邮箱、凭据、令牌、私有路径和超长文本模式。模式检查可以降低意外泄露，但不能证明任意文本一定安全。分享前，用户仍需自行检查每个下载文件。

## Demo 数据

内置场景仅用于说明。其来源与隐私字段明确声明不包含真实科研结果。`privacy.sanitization.review_status: approved` 表示独立审计检查了某个特定候选快照，并不保证后续修改或用户输入自动安全。

## 问题反馈

安全敏感的隐私问题请遵循 [SECURITY.zh-CN.md](SECURITY.zh-CN.md)，不要把具体细节提交到公开 Issue。
